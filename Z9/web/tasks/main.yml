---
# tasks file for web
- name: install packages web
  apt: name={{item}} status=installed
  with_items:
    - default-jdk
    - tomcat9

- name: Ensure tomcat star
      service:
       name: tomcat9
       state: started